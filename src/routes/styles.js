import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    activeIcon: {
        height: 25,
        width: 25,
    },
    icon: {
        height: 25,
        width: 25,
        opacity: 0.5
    }
})

export default styles