import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  socialBtn: {
    height: 40,
    width: 40,
    borderRadius: 50,
    marginHorizontal: 20,
  },
});

export default styles;